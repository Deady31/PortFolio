# CV — only the FR PM CV is downloadable from the site
uploads/*.docx
uploads/*.doc
uploads/DETAIL*.pdf

# Build output and dependencies (only published via GitHub Actions, never committed)
node_modules/
dist/
package-lock.json

# Old versions
portfolio.html
portfolio-print.html
portfolio_v1_archive.html
portfolio_upgraded - Copie.html
